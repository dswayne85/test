"""Low-level GUI helpers (waiters, keyboard, screenshots, selectors, connect).

Modules here may use pywinauto, but they import it lazily so that importing the
package does not require a Windows environment.
"""
